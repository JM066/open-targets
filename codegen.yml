schema: 'src/graphql/schema.graphql'
documents: 'src/**/*.{ts,tsx}'
generates:
  src/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      useTypeImports: true
      enumsAsTypes: true
      skipTypename: true
